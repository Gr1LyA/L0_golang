запуск nats сервера
    sudo docker run -ti -p 4222:4222 nats-streaming

запуск докер контейнера с базой данных
    sudo docker run -e POSTGRES_PASSWORD="1234" -p 5437:5432 -d --rm postgres

подключение к заапущенному контейнеру
    sudo docker exec -it container_id /bin/bash

    Подключение к БД:
        psql -U postgres

создание таблицы
    postgres=# create table db(
    uid text,
    data json);
    CREATE TABLE

    postgres=# \d db
                   Table "public.db"
     Column | Type | Collation | Nullable | Default
    --------+------+-----------+----------+---------
     uid    | text |           |          |
     data   | json |           |          |
